import { NextResponse } from "next/server";

export async function POST() {
  // Placeholder for Supabase signed upload URL flow.
  return NextResponse.json({ message: "Implement signed URL upload with Supabase Storage in production." });
}
